node_modules/
.next/
out/
.env*
!.env.example
*.tsbuildinfo
next-env.d.ts
test-results/
playwright-report/
public/sw.js
supabase/.temp/
.vercel/
.superpowers/

# Task 4 (security probe runner): the pre-judgement checkpoint file. Real
# job is inside a single run (self-check re-reads it to prove judge() is
# reproducible from disk) -- byte-identical to ledger.json's own `outcome`
# across all 1824 rows minus the `verdict` field, so tracking it in git is
# pure duplication. ledger.json (the actual LedgerRow[] Tasks 6-9 consume)
# stays tracked -- its git history IS the audit's evidence trail.
docs/security/ledger-raw.json

# Georgian byte-splicing working material (see the Georgian rule in DESIGN.md).
# Regenerated on demand from the reference bundle; never a deliverable.
scratch/
